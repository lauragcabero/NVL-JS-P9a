var miArray = [['a', 'b'], ['c', 'd'], ['e', 'f']];

// Indica la longitud del array:

var longitud = miArray.length;

// test

console.log(longitud);

// Indica la longitud de uno de los elementos del array y test:

console.log(miArray[1].length);

// Muestra por pantalla el valor del tercer elemento de la lista del array. Y dentro de ese tercer elemento, su segundo elemento:

document.write(miArray[2][1]);